import BaseToolState from './BaseToolState';

class BaseLineToolState extends BaseToolState {
  constructor() {
    super();

    this.width = 1;
    this.color = '#000000';
  }
}

export default BaseLineToolState;
